runConfig:
  cpu: 1
  memoryMiB: 512
  concurrency: 50
  minInstances: 0

# Default (safe) config. Environment-specific overrides should live in:
# - apphosting.<ENVIRONMENT_NAME>.yaml
#
# For Firebase App Hosting environments, set the backend "Environment name"
# in the console (Settings -> Environment). App Hosting will merge:
#   apphosting.yaml + apphosting.<ENV>.yaml (overrides win)
#
# Docs: https://firebase.google.com/docs/app-hosting/multiple-environments


