// Example Firebase config file (SAFE to commit; uses placeholders).
// Create `src/assets/firebase-config.js` locally (gitignored) OR generate it via
// `node scripts/generate-firebase-config.mjs` (runs automatically on npm start/build).
//
// The app reads the config from `window.__FIREBASE_WEB_CONFIG__`.
window.__FIREBASE_WEB_CONFIG__ = {
  apiKey: 'API_KEY',
  authDomain: 'PROJECT_ID.firebaseapp.com',
  projectId: 'PROJECT_ID',
  storageBucket: 'PROJECT_ID.firebasestorage.app',
  messagingSenderId: 'SENDER_ID',
  appId: 'APP_ID',
  measurementId: 'G-MEASUREMENT_ID',
};


